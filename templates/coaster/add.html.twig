{% extends 'base.html.twig' %}

{% block body %}
<h1>Ajouter un coaster</h1>
{{ form_start(coasterForm) }}
{{ form_widget(coasterForm) }}
{# button:submit.btn.btn-primary #}
<button type="submit" class="btn btn-primary">Ajouter</button>
{{ form_end(coasterForm) }}

{% endblock %}